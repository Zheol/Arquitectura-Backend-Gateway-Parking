syntax = "proto3";

package zones;

service ZonesService {
    rpc findAll (Empty) returns (Zones);
}

message Empty {}

message Zones {
    int32 id = 1;
    string name = 2;
    string cant_estacionamientos_totales = 3;
    int32 cant_estacionamientos_ocupados = 4;
}